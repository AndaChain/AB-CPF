<h1> <%= @department.name%> </h1>

<%= button_to "Log out", logout_path, class: 'btn logoutbtn', method: "delete" %>


<div>
    <% ShiftTime.all.each.with_index(1) do |a| %>
    <h2>
        <%= button_to  "Edit", manage_shifttime_path(manage_id:@department.code, id:a.id), class: 'btn editbtn', method: "get" %>
        00:00 - 09.00 
        <br>
            4/5
        </br>
    </h2>
    <%end%>
    <h2>
        <%= button_to  "+", manage_path(params[:id]), class: 'btn', method: "get" %>
    </h2>
</div>
